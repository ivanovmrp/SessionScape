policy:
  # User-invoked only (mirrors Claude Code's disable-model-invocation): init and
  # re-validation are deliberate acts the user triggers via /skills or $sdlc-init.
  allow_implicit_invocation: false
